name: 'Cleanup MATI Environment'
description: 'Clean up build artifacts and temporary files'
runs:
  using: "composite"
  steps:
    - name: Clean up
      shell: bash
      run: |
        yes | make clean || true
